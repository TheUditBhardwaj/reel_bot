# reel_bot
The system should allow a user to send an Instagram reel URL/ caption to a bot. The backend should process the reel, extract meaningful information, generate an AI summary, and send the structured response back to the user automatically.
